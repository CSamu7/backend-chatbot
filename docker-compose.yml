services:
  db-mysql:
    image: mysql:oraclelinux9
    container_name: "chatbot-db"
    restart: always
    env_file:
      - ./.env
    environment:
      - MYSQL_DATABASE
      - MYSQL_ROOT_PASSWORD
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 3s
      retries: 5


  backend:
    container_name: myapp
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - 8000:8000
    volumes:
      - .:/app/backend
    depends_on:
      db-mysql:
        condition: service_healthy
    networks:
      - app-network

  

  # frontend:
  #   container_name: frontend
  #   stdin_open: true
  #   command: npm run dev
  #   build: 
  #     context: ./frontend  # Esta carpeta puede no existir
  #     dockerfile: Dockerfile
  #   volumes:
  #     - ./frontend:/usr/src/app
  #     - /usr/src/app/node_modules
  #   ports:
  #     - "5173:5173"
  #   environment:
  #     - NODE_ENV=development
  #     - VITE_API_URL=http://backend:8000
  #     - VITE_CHATBOT_URL=http://chatbot-client:8501
  #   networks:
  #     - app-network
  #   depends_on:
  #     - backend
  #     - chatbot-client

  
  # reverse_proxy:
  #   image: "nginx:stable-perl"
  #   container_name: reverse-proxy
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "81:81"
  #     - "443:443"
  #   volumes:
  #     - ./data:/data
  #     - ./letsencrypt:/etc/letsencrypt
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   networks:
  #     - app-network
  #   depends_on:
  #     - frontend
  #     - backend
  #     - chatbot-client

networks:
  app-network:
    driver: bridge

volumes:
  mysql_data:
  ml_models: